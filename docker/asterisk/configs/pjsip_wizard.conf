;============================================================================
; VoziPOmni Contact Center - PJSIP Wizard Configuration
;============================================================================
; Este archivo es generado automáticamente por el sistema VoziPOmni
; cuando se crean o modifican troncales desde la interfaz web.
;
; NO EDITAR MANUALMENTE - Los cambios serán sobrescritos
;
; Las troncales se configuran desde:
;   Configuración > Telefonía > Troncales SIP
;
; El sistema utiliza PJSIP Wizard para simplificar la configuración
; de troncales con diferentes proveedores y escenarios (NAT, sin NAT,
; PBX en LAN, corporativas, etc.)
;============================================================================

; Las configuraciones de troncales aparecerán aquí automáticamente
; cuando se creen desde la interfaz web

