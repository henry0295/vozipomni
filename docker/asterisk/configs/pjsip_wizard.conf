;============================================================================
; VoziPOmni Contact Center - PJSIP Wizard Configuration
;============================================================================
; Este archivo es leído por res_pjsip_config_wizard.so
; Incluye las configuraciones dinámicas generadas desde la interfaz web.
;
; NO EDITAR MANUALMENTE - Los cambios serán sobrescritos
;============================================================================

; Incluir las troncales generadas dinámicamente por el backend
#include /var/lib/asterisk/dynamic/pjsip_wizard.conf
