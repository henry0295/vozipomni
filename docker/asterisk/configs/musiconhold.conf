;============================================================================
; VoziPOmni Contact Center - Music On Hold Configuration
; Las clases MOH se configuran desde la interfaz web
;============================================================================

[default]
mode=files
directory=/var/lib/asterisk/moh

; Incluir clases MOH dinámicas generadas por Django
#include /var/lib/asterisk/dynamic/musiconhold_dynamic.conf
