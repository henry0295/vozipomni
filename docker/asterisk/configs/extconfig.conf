; extconfig.conf - External Configuration
; Configura Asterisk para usar Redis como backend de configuración

[settings]
; SIP Peers y Users desde Redis
sippeers => redis,sippeers
sipusers => redis,sipusers

; Colas y miembros desde Redis
queues => redis,queues
queue_members => redis,queue_members

; Voicemail desde Redis
voicemail => redis,voicemail

; Extensions (opcional, se puede dejar en archivos)
; extensions => redis,extensions

; Variables globales de campañas en Redis
; Esto permite que Django actualice parámetros de campaña en tiempo real
; y Asterisk los lea desde Redis
